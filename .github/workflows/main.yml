name: Build luci-app-dnscrypt-proxy2 for OpenWrt/ImmortalWrt

on:
  workflow_dispatch:
    inputs:
      firmware_type:
        description: "Firmware type"
        type: choice
        required: true
        default: "openwrt"
        options:
          - "openwrt"
          - "immortalwrt"
      openwrt_version:
        description: "OpenWrt/ImmortalWrt version (e.g., 24.10.4 or SNAPSHOT)"
        type: string
        required: true
        default: "24.10.4"
      openwrt_target:
        description: "Target (e.g., mediatek)"
        type: string
        required: true
        default: "mediatek"
      openwrt_subtarget:
        description: "Subtarget (e.g., filogic)"
        type: string
        required: true
        default: "filogic"
      openwrt_arch:
        description: "Architecture (e.g., aarch64_cortex-a53)"
        type: string
        required: true
        default: "aarch64_cortex-a53"

jobs:
  build-package:
    name: "Build LuCI for ${{ inputs.firmware_type }}: ${{ inputs.openwrt_version }} - ${{ inputs.openwrt_target }}/${{ inputs.openwrt_subtarget }}"
    runs-on: ubuntu-22.04
    defaults:
      run:
        shell: bash
        
    steps:
        # 1. Клонирование репозитория
    - name: Checkout repository
      uses: actions/checkout@v5
      with:
        submodules: true
        
    - name: Install dependencies
      run: |
        set -e -x
        sudo apt-get update
        sudo apt-get install -y build-essential gawk git python3 python3-setuptools rsync unzip wget xz-utils zstd
        
    - name: Extract package version from Makefile and set release tag
      id: pkg_version
      run: |
        set -e -x
        # Ищем версию в Makefile LuCI-пакета
        PKG_VERSION=$(grep '^PKG_VERSION:=' Makefile | cut -d= -f2 | tr -d '[:space:]')
        if [ -z "$PKG_VERSION" ]; then
          echo "❌ Cannot find PKG_VERSION in Makefile"
          exit 1
        fi
        echo "PKG_VERSION=$PKG_VERSION" >> $GITHUB_ENV
        TAG_NAME="luci-app-dnscrypt-proxy2-${{ inputs.firmware_type }}-${{ inputs.openwrt_version }}-${{ inputs.openwrt_target }}-${{ inputs.openwrt_subtarget }}-${PKG_VERSION}"
        echo "tag_name=$TAG_NAME" >> $GITHUB_ENV
        echo "release_name=DNSCrypt Proxy LuCI Interface ${{ inputs.firmware_type }}-${{ inputs.openwrt_version }} for ${{ inputs.openwrt_arch }}" >> $GITHUB_ENV
        
    - name: Prepare SDK and Configure Build 🛠️
      run: |
        set -e -x
        
        FIRMWARE_TYPE="${{ inputs.firmware_type }}"
        VERSION="${{ inputs.openwrt_version }}"
        TARGET="${{ inputs.openwrt_target }}"
        SUBTARGET="${{ inputs.openwrt_subtarget }}"
        
        # 1. Определение URL SDK
        if [ "$FIRMWARE_TYPE" = "openwrt" ]; then
          BASE_DOMAIN="downloads.openwrt.org"
          SDK_PATTERN="openwrt-sdk"
        else
          BASE_DOMAIN="downloads.immortalwrt.org"
          SDK_PATTERN="immortalwrt-sdk"
        fi
        
        if [ "$VERSION" = "SNAPSHOT" ]; then
          BASE_URL="https://${BASE_DOMAIN}/snapshots/targets/${TARGET}/${SUBTARGET}/"
        else
          BASE_URL="https://${BASE_DOMAIN}/releases/${VERSION}/targets/${TARGET}/${SUBTARGET}/"
        fi
        
        echo "🔍 Searching SDK at: ${BASE_URL}"
        
        # 2. Загрузка и распаковка SDK
        sdk_url=$(curl -s "${BASE_URL}" | grep -oP "href=\"${SDK_PATTERN}[^\"]+\.tar\.(xz|zst|gz|bz2)\"" | sed -e 's/href="//' -e 's/"$//' | head -n1)
        
        if [[ -z "$sdk_url" ]]; then
          echo "❌ SDK file not found at ${BASE_URL}"
          exit 1
        fi
        
        curl -fsLO "${BASE_URL}${sdk_url}" || { echo "❌ Failed to download SDK"; exit 1; }
        file_name=$(basename "$sdk_url")
        
        tar -xf "$file_name" || { echo "❌ Failed to extract SDK"; exit 1; }
        rm -f *.tar.*
        
        SDK_DIR=$(find . -maxdepth 1 -type d -name "*${SDK_PATTERN}*" | head -1)
        mv "$SDK_DIR" sdk
        echo "✅ SDK prepared in ./sdk/"
        
        # 3. Копирование minisign и LuCI-пакета в SDK/package/
        echo "📦 Copying minisign and LuCI package to SDK/package/..."
        
        # Копируем minisign (убедитесь, что он в ./minisign/)
        cp -r minisign sdk/package/
        
        # Копируем LuCI-пакет
        mkdir -p sdk/package/luci-app-dnscrypt-proxy2
        rsync -av --exclude 'sdk' --exclude 'minisign' --exclude '.git' --exclude '.github' . sdk/package/luci-app-dnscrypt-proxy2/
        echo "✅ All required packages copied to SDK"
        
        # 4. Обновление фидов с повторными попытками
        cd sdk
        
        MAX_ATTEMPTS=5
        for attempt in $(seq 1 $MAX_ATTEMPTS); do
            echo "🔄 Updating feeds (Attempt $attempt/$MAX_ATTEMPTS)..."
            if ./scripts/feeds update -a; then
                echo "✅ Feeds updated successfully."
                break
            else
                if [ $attempt -eq $MAX_ATTEMPTS ]; then
                    echo "❌ Failed to update feeds after $MAX_ATTEMPTS attempts."
                    exit 1
                fi
                echo "⏳ Waiting 15 seconds before retry..."
                sleep 15
            fi
        done
        
        echo "📥 Installing required packages..."
        
        # Устанавливаем dnscrypt-proxy2 из feeds
        ./scripts/feeds install dnscrypt-proxy2 || { echo "❌ Failed to install dnscrypt-proxy2"; exit 1; }
        
        # Устанавливаем libsodium (зависимость minisign)
        ./scripts/feeds install libsodium || { echo "❌ Failed to install libsodium"; exit 1; }
        
        # Устанавливаем зависимости LuCI
        ./scripts/feeds install luci-compat luci-lib-ip luci-lua-runtime || { echo "❌ Failed to install luci deps"; exit 1; }
        
        echo "✅ Base dependencies installed"
        
        # 🔧 ВАЖНО: Модифицируем libsodium ПОСЛЕ установки feeds
        echo "🔧 Modifying libsodium to enable full API..."
        LIBSODIUM_MAKEFILE="feeds/packages/libs/libsodium/Makefile"
        
        if [ -f "$LIBSODIUM_MAKEFILE" ]; then
            echo "📝 Found libsodium Makefile at: $LIBSODIUM_MAKEFILE"
            
            # Показываем оригинальное содержимое
            echo "📋 Original configure options:"
            grep "CONFIGURE_ARGS" "$LIBSODIUM_MAKEFILE" || echo "⚠️ No CONFIGURE_ARGS found"
            
            # Модифицируем Makefile
            sed -i 's/--enable-minimal/--enable-minimal=no/g' "$LIBSODIUM_MAKEFILE"
            
            echo "✅ libsodium Makefile modified to enable full API"
            echo "📋 Modified configure options:"
            grep "CONFIGURE_ARGS" "$LIBSODIUM_MAKEFILE" || echo "⚠️ No CONFIGURE_ARGS found after modification"
        else
            echo "⚠️ WARNING: libsodium Makefile not found at $LIBSODIUM_MAKEFILE"
            echo "📂 Searching for libsodium Makefile..."
            find feeds -name "Makefile" -path "*/libsodium/*" -ls || echo "❌ No libsodium Makefile found in feeds"
            
            # Пытаемся найти и использовать альтернативный путь
            ALT_MAKEFILE=$(find feeds -name "Makefile" -path "*/libsodium/*" | head -n1)
            if [ -n "$ALT_MAKEFILE" ]; then
                echo "✅ Found alternative libsodium Makefile at: $ALT_MAKEFILE"
                sed -i 's/--enable-minimal/--enable-minimal=no/g' "$ALT_MAKEFILE"
                echo "✅ Alternative Makefile modified"
            else
                echo "❌ Failed to find libsodium Makefile anywhere"
                exit 1
            fi
        fi
        
        # Загрузка config.buildinfo
        echo "⚙️  Downloading base config..."
        curl -fsL "${BASE_URL}/config.buildinfo" > .config || { echo "⚠️  config.buildinfo not found, using default"; }
        
        # Явно включаем пакеты
        echo "CONFIG_PACKAGE_minisign=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-dnscrypt-proxy2=y" >> .config
        
        # Включение зависимостей Minisign
        echo "CONFIG_PACKAGE_libsodium=y" >> .config
        echo "CONFIG_PACKAGE_libpthread=y" >> .config
        
        make defconfig
        echo "✅ Build configuration completed"

    - name: Build Minisign Package
      run: |
        set -e -x
        cd sdk
        echo "🔨 Building minisign package..."
        make package/minisign/compile V=s -j $(nproc) || { echo "❌ Failed to build minisign"; exit 1; }
        
        # Установка minisign в SDK (важно для зависимостей)
        make package/minisign/install V=s || { echo "❌ Failed to install minisign"; exit 1; }
        echo "✅ Minisign package built and installed"
        
        # Проверяем, что пакет создан
        echo "📦 Built minisign packages:"
        find bin -name "*minisign*.ipk" -ls || echo "⚠️  No minisign IPK found yet"
        
    - name: Index packages (make minisign available)
      run: |
        set -e -x
        cd sdk
        echo "📇 Indexing packages..."
        make package/index V=s || { echo "❌ Failed to index packages"; exit 1; }
        echo "✅ Packages indexed"
        
    - name: Compile LuCI Package
      run: |
        set -e -x
        cd sdk
        echo "🔨 Compiling luci-app-dnscrypt-proxy2..."
        make package/luci-app-dnscrypt-proxy2/compile V=s -j $(nproc) || { echo "❌ Failed to compile LuCI package"; exit 1; }
        echo "✅ LuCI package compiled successfully"
        
    - name: Prepare artifacts for release
      id: prepare_artifacts
      run: |
        set -e -x
        mkdir -p release
        
        echo "📦 Collecting built packages..."
        # Собираем minisign и LuCI-пакет
        find sdk/bin -type f -name "*luci-app-dnscrypt-proxy2*.ipk" -exec cp {} release/ \;
        find sdk/bin -type f -name "*minisign*.ipk" -exec cp {} release/ \;
        
        IPK_COUNT=$(find release -type f -name "*.ipk" | wc -l)
        if [ "$IPK_COUNT" -eq 0 ]; then
          echo "❌ No IPK files found! Build may have failed."
          echo "Looking for packages in sdk/bin:"
          find sdk/bin -name "*.ipk" -ls
          exit 1
        fi
        
        echo "✅ Found $IPK_COUNT package(s):"
        ls -lh release/
        
    - name: Get current date for release notes
      id: get_date
      run: echo "date=$(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_OUTPUT
      
    - name: Create Release and Upload Assets
      uses: softprops/action-gh-release@v2
      with:
        files: release/*
        tag_name: ${{ env.tag_name }}
        name: ${{ env.release_name }}
        body: |
          ## 📦 Автоматическая сборка пакетов DNSCrypt Proxy
          
          Этот релиз содержит **minisign** и **luci-app-dnscrypt-proxy2**, собранные для целевой архитектуры **${{ inputs.openwrt_arch }}**.
          
          ### ⚠️ Важно
          - Демон **dnscrypt-proxy2** должен быть установлен из официального репозитория OpenWrt/ImmortalWrt
          - Пакет **minisign** собран из исходников (C++ версия) для данной архитектуры
          - Minisign предназначен для использования с пользовательскими списками резолверов
          
          ### 📋 Информация о сборке
          - **Дата сборки:** ${{ steps.get_date.outputs.date }}
          - **Firmware:** ${{ inputs.firmware_type }} ${{ inputs.openwrt_version }}
          - **Target:** ${{ inputs.openwrt_target }}/${{ inputs.openwrt_subtarget }}
          - **Архитектура:** ${{ inputs.openwrt_arch }}
          - **Версия пакета:** ${{ env.PKG_VERSION }}
          
          ### 📥 Установка
          ```bash
          # Сначала установите minisign
          opkg install minisign_*.ipk
          
          # Затем установите LuCI-приложение
          opkg install luci-app-dnscrypt-proxy2_*.ipk
          ```
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
